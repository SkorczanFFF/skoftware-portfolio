/**
 * Screen-reader and alt text that would otherwise hide inside components.
 * `{title}` and `{days}` are filled by the consumer.
 */
export type A11yStrings = {
  opensInNewTab: string;
  screenshotOf: string;
  screenshotAltView: string;
  scrollToTop: string;
  scrollToServices: string;
  backToHome: string;
  switchLanguage: string;
  techStripLabel: string;
  portfolioLabel: string;
};
